spring.application.name: order-management-service

spring:
  docker:
    compose:
      enabled: false # Setting docker environment to false for production
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1
